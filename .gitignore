# v3 board data is committed to the dedicated Pages repository. Never put
# secrets or token-bearing files in this folder or repository.
# These are local-only runtime / secret-bearing artifacts — never commit them:
*.token
token.txt
data/*.local.json
data/*.local.js
serve.log
.github/skills/**/config.local.psd1
